import ControlledAccordion from "./components/ControlledAccordion";

/** The main component, which is displayed in the root-route. */
function App() {
  return <ControlledAccordion></ControlledAccordion>;
}

export default App;
